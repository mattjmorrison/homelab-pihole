apiVersion: batch/v1
kind: Job
metadata:
  name: pihole-verify
  namespace: pihole
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: verify
          image: "{{ .Values.verify.image.repository }}@{{ .Values.verify.image.digest }}"
          command: ["/bin/sh", "/scripts/verify.sh"]
          env:
            - name: PIHOLE_ADDR
              value: {{ .Values.piholeAddress | quote }}
          volumeMounts:
            - name: script
              mountPath: /scripts
      volumes:
        - name: script
          configMap:
            name: pihole-verify-script
            defaultMode: 0555
